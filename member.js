/**
 * Returns a string listing the skills of the member.
 * 
 * @returns {string} A string that lists the skills of the member, separated by commas.
 */
function skillsMember() {
    return 'Skills: ' + this.skills.join(', ');
    document.write('Skills: ' + this.skills.join(', '));
    }
